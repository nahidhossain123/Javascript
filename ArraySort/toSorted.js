//toSorted--->safe way to sort an array without altering the original array
//ES23 feature
const months = ["Jan", "Feb", "Mar", "Apr"];
const sorted = months.toSorted();
console.log("toSorted method", sorted);
