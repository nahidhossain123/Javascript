//flat() --->convert multi-dimensinal arrya into a one dimensinal array
const myArr = [
  [1, 2],
  [3, 4],
  [5, 6],
];
newArr = myArr.flat();
console.log("two dimensional to one dimensinal:", newArr);

//browser support
//ES19 introduced the array flat() method
