//unshift()---> add new element at the beginning  of the array and unshift all other array elements
//return the new lenght of the array
const fruits = ["Banana", "Orange", "Apple", "Mango"];
let fruit = fruits.unshift("Lemon");
console.log(
  `after unshift new array is=${fruits} and array length is=${fruit}`
);
